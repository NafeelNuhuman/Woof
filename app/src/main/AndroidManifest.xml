<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.woof">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".other.Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Nethmini -->
        <activity android:name=".stories.mystories" />
        <activity android:name=".stories.stories" />
        <activity android:name=".stories.singleStoryView"></activity>
        <activity android:name=".stories.stories2" />

        <!-- Nimran -->
        <activity android:name=".dogs.selectedDogview"></activity>
        <activity android:name=".dogs.viewAllpets" />
        <activity android:name=".dogs.viewMypet" />
        <activity android:name=".dogs.uploadPet" />
        <activity android:name=".dogs.addDog" />

        <!-- Atchu -->
        <activity android:name=".reviews.clickReview"></activity>
        <activity android:name=".reviews.reviewItem"></activity>
        <activity android:name=".reviews.editReview"></activity>

        <!-- Nafeel -->
        <activity android:name=".Accessories.updateAccessory" />
        <activity android:name=".Accessories.manageAccessories">

        </activity>
        <activity android:name=".Accessories.uploadAccessory"></activity>
        <activity android:name=".Accessories.Accesories" />
        <activity android:name=".Accessories.singleItemView"></activity>
    </application>

</manifest>